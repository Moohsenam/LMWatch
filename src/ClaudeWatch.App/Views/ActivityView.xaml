<UserControl x:Class="ClaudeWatch.App.Views.ActivityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="24,4,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding L[Act_Title]}" Style="{StaticResource Display}" FontSize="20" />
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="{Binding L[Act_Hint]}" Style="{StaticResource Dim}" />
                    <TextBlock Text="{Binding Edit.LogRetentionDays}" Style="{StaticResource Dim}" Margin="4,0,4,0" />
                    <TextBlock Text="{Binding L[Act_Days]}" Style="{StaticResource Dim}" />
                </StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <Button Content="{Binding L[Btn_Export]}" Style="{StaticResource SecondaryButton}"
                        Command="{Binding ExportLogCommand}" Margin="0,0,8,0" />
                <Button Content="{Binding L[Btn_Clear]}" Style="{StaticResource GhostButton}"
                        Command="{Binding ClearLogCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14">
            <Grid>
                <TextBlock Text="{Binding L[Act_Empty]}" Style="{StaticResource Dim}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding Events.Count, Converter={StaticResource EmptyVis}}" />

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Events}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource Row}" Background="Transparent" BorderThickness="0"
                                        Padding="10,8" Margin="0,0,0,2">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Width="4" CornerRadius="2" Margin="0,1,12,1"
                                                Background="{Binding Kind, Converter={StaticResource KindBrush}}" />

                                        <TextBlock Grid.Column="1" Text="{Binding TimeDisplay}"
                                                   Style="{StaticResource Mono}" VerticalAlignment="Center"
                                                   Foreground="{DynamicResource TextFaint}" Margin="0,0,14,0" />

                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" Style="{StaticResource Body}" />
                                            <TextBlock Text="{Binding Detail}" Style="{StaticResource Caption}"
                                                       Margin="0,2,0,0"
                                                       Visibility="{Binding Detail.Length, Converter={StaticResource EmptyVis}, ConverterParameter=invert}" />
                                        </StackPanel>

                                        <TextBlock Grid.Column="3" Text="{Binding DateDisplay}"
                                                   Style="{StaticResource Caption}" VerticalAlignment="Center" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
